# S4 gate report — inference layer

**Stage:** S4 · **Written:** 2026-07-29 · **Status: Block 1 GREEN, Blocks 2–5 not started**

---

# 1. The A2 ruling your freeze waits on — RESOLVED (Session 12)

`docs/data_sources.md` carries the full reasoning. In short: the SEIBro field is
**programme-specific issuance-ceiling headroom** (`ceiling − outstanding`, revolving),
established from 502 up / 757 down moves with **zero** unchanged across 1,260 observations,
in block-sized steps, plus the capped programme reading exactly 0 after the full board
authorization went to the depositary.

**The tension it exposed, and your ruling on it:** measured headroom and the operative
consent gate can diverge, so headroom can move without the barrier opening. You ruled
**option (a)** — scope H5 to the measurable claim and add an **INDETERMINATE** branch.
Applied in `preregistration/proposed_h5_values.md`, which now carries four resolution
branches. **Your freeze is unblocked.**

# 2. Block 1 — the hard gate: GREEN

`just validate` (or `uv run python -m scripts.validate`) runs it standalone.

| Component | Status |
|---|---|
| Expanding walk-forward splitter | ✅ training never reaches past the test block; expands monotonically |
| Purged K-fold + embargo | ✅ no train label window touches test; every obs tested once; purge cost surfaced per fold |
| Leaky-scaler detection | ✅ demonstrated against a full-sample fixture |
| Availability-timestamp assertion | ✅ boundary exclusive — a feature published *at* the decision instant is not usable |
| Filtered-vs-smoothed canary | ✅ smoothed probabilities rejected for prediction |
| SKHY structural exclusion | ✅ raises on any fitting index containing it |

**21 tests.** Each detector is shown a real leak rather than merely imported — a detector
nobody has demonstrated a leak to is a detector nobody knows works.

**Two Block-1 spec items remain unbuilt:** the fold-scoped-only fit API (making the leaky
construction impossible rather than merely detected), and per-pair time indexing in the
splitters. The current detectors catch leaks; the fit API would prevent them.

# 3. Open questions from the master checklist — settled

| Question | Answer |
|---|---|
| **Intraday 07-28→29** | ❌ **PERMANENTLY LOST.** Never captured; Yahoo was throttled throughout the window and free retention has since aged it out. The F3 decomposition will run at daily resolution and says so. |
| **D3 / KOFIA puller** | ❌ Not built. KOFIA is an eXBuilder6 SPA whose XHR payload was never reverse-engineered; no approved alternative has landed. |
| **D4 history-vs-snapshot** | ❓ **Still unresolved, and now costly.** No KODEX/TIGER/Naver capture has ever run, so the question "is AUM history retrievable or snapshot-only?" is unanswered — and if snapshot-only, every day since approval is lost. **This is the most time-sensitive open item in the repo.** |
| **F3 poll** | Armed 03:19 UTC, alive, fires 20:05 UTC. |

# 4. Not started

Blocks 2 (M5/M6 features), 3 (M2/M3 under quarantine), 4 (engines + probes) and 5
(G10/G11, notebooks 04/05). Block 4's H4 and H5 engines exist from Session 12 but have not
been run under the validation layer.

**No models were fitted this session**, so there is no metrics table, no ablation, and no
H3 decision-capable date. S15's trade sheets are gated on the metrics table existing, so
that gate is **not** satisfied.

# 5. Named constants awaiting ratification

| Constant | Current | Where |
|---|---|---|
| Embargo periods | not pinned | `purged_kfold(embargo=...)` — caller-supplied |
| Label horizon `H` | not pinned | `purged_kfold(label_horizon=...)` |
| Sign-prior strength | not defined | M3 not built |
| FX-leg default | `TODO(ash: ratify)` | `premium.DEFAULT_FX_LEG` |
| Close-definition default | `TODO(ash: ratify)` | `premium.DEFAULT_CLOSE_DEF` |

# 6. Your checklist

Freeze (unblocked — three fields) · ratifications (none) · README §0–§5 wording
confirmation (a session edited a reserved file on your override) · India email (evidence
assembled) · **both keys unrotated** · social card ready to upload · **D4 approval
decision — the perishable one**.
